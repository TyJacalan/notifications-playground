<%= turbo_stream.update 'toast', partial: 'layouts/toast' %>
<%= turbo_stream.replace 'comment_form', partial: 'comments/form', locals: { comment: Comment.new, post: @comment.post } %>
<%= turbo_stream.append "comments_#{dom_id(@comment.post)}", partial: 'comment', locals: { comment: @comment, post: @comment.post } %>